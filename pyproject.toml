[tool.poetry]
name = "qudi-hira-analysis"
version = "1.0.0"
description = "A Python toolkit to analzye photon timetrace data from qubit sensors"
authors = ["dineshpinto <dkpinto95@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "qudi-hira-analysis"}]

[tool.poetry.dependencies]
python = "^3.10"
lmfit = "^1.1.0"
matplotlib = "^3.6.2"
numpy = "^1.24.0"
pandas = "^1.5.2"
seaborn = "^0.12.1"
jupyterlab = "^3.5.2"
xlrd = "^2.0.1"
openpyxl = "^3.0.10"
tqdm = "^4.64.1"
pyspm = { git = "https://github.com/dineshpinto/pySPM.git" }


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
